# Explicit source-root design review

Guarded dual analysis `6cb05b6c-a595-4074-a6b2-cf690bae8ac5` completed with Codex REQUEST_CHANGES and Claude APPROVE. Both returned terminal reports. Codex required complete cache identity; both requested precise duplicate and symlink behavior. The amended plan retains and canonicalizes existing workspace/aggregate identity, adds extra-root identity, and additionally separates compiler-host memoization by aggregate. It explicitly keeps the immutable-snapshot cache contract rather than pretending file mutation is supported.

Conflicting physical roots for a package-name/compiler-face identity are rejected globally in the admitted direct-reference inventory, even during focused emission. Out-of-container symlink targets remain excluded; explicitly allowed sibling containers are admitted. Host/Client aggregates stay independent, only product Host artifacts are emitted, and no temporary aggregate directory may enter outputs. Missing-symbol rejection changes require diagnosis after resolution is corrected. These are lead dispositions pending the follow-up design verdict; no code approval is claimed.

Follow-up `e241736e-51e9-4aa8-bfa4-5aa5b87a0461` returned APPROVE from both models, with no Critical findings. Claude noted per-run temporary-config cache accumulation: the product creates a fresh generator/cache for one generation call and releases it when the invocation returns, never keeping a process-wide snapshot. Additional roots affect inventory only, while aggregate compiler options own module resolution. Output paths retain the product workspace root; reject any temporary-directory leakage instead of rewriting paths to conceal a resolution defect. These verdicts approve the design only.

## Implementation evidence

The new external-workspace file and adjacent Remote-model regressions passed together: 2 files, 50 tests, 34.21 seconds. The new tests include real sibling protocol sources, canonical containment, global duplicate rejection, aggregate cache separation, default generation and invalid request rejection. Reverting the compiler-host cache key caused the two cache regressions to fail. The fixture-only protocol directory was renamed to match an existing repository path so the package-path gate does not mistake a temporary path for a broken repository link.

Implementation review `7c73f433-84e1-4f41-bf82-9fa8ab52a9ce` requested changes. The three-argument exported cache method is an intentional pre-release API change, now documented with its migration. The suspected missing-config failure was refuted by the existing `realPath` helper, which returns unresolved absolute paths before calling `realpathSync` when the file is absent. Diagnostic common-root inference must cover primary packages importing sibling sources, not only packages outside the primary container; no-emit scope and unchanged semantic checks are now explicit in the README.

Re-review `52b2d689-6816-4fce-a80f-bbb4c9850ca1` returned APPROVE from both models, with no Critical or Warning findings. This approves the generator implementation, not the product's pending actual WSL generation or Web launch. The task remains open until the real paired source-generation probe succeeds. Initial doc-sync passed 30 of 32 gates; the fixture path check now passes and the mechanically refreshed event graph corrects line references shifted by the already-present native event patch. Remaining check outcomes are recorded at completion.

The doc graph recheck passed (six graphs current), as did the corrected package-path gate and translation pairing. The required full typed lint exposed eleven existing findings in the native-event patch. Focused cleanup preserves thenable rejection and nullish-registration diagnostics, declares receiver-free validators, removes a redundant cast and awaits asynchronous effect disposal in JSONL tests. Two files passed 149 Session/JSONL tests; after the nullish negative cases, the affected 12 Session tests and changed-file typed lint passed again. The rest of the full lint scan had no findings; it was not repeated solely for commit. Cleanup review `7bd8ead9-7aac-4581-8861-34b52fe35668` requested the restored nullish guard; re-review `12c7654b-e936-445c-b217-8f79b1800f82` returned APPROVE from both models. These changes remain on the explicitly temporary development fork, not an official public API release.
